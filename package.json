{
  "name": "mindshift-landing",
  "version": "1.0.0",
  "private": true,
  "description": "Mindshift Labs landing page — static build",
  "scripts": {
    "build": "babel src/app.jsx --presets @babel/preset-react --minified --out-file assets/app.js"
  },
  "devDependencies": {
    "@babel/cli": "^7.28.0",
    "@babel/core": "^7.29.0",
    "@babel/preset-react": "^7.27.1"
  }
}
